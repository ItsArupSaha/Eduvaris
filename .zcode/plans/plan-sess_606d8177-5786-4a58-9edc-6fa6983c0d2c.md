## Bundle: "Buy All Modules" — 185 BDT for all 4 (APPROVED)

User approved 100%. Implementing all 12 changes now.

### Data model: discriminator fields `isBundle` + `modules`
- Bundles: `module:"bundle"`, `isBundle:true`, `modules:[reading,listening,writing,speaking]`, `amount:BUNDLE_PRICE(185)`
- Singles (unchanged): `module:"reading"`, no isBundle/modules, `amount:MODULE_PRICE(50)`
- Grant logic: `const targets = data.modules ?? [data.module]`, loop increment inside txn

### 12 changes
1. config.ts — add BUNDLE_PRICE (185) + isBundleMode validator
2. payment-types.ts — add isBundle?:boolean + modules?:ModuleKey[] fields, MODULE_BUNDLE const, isBundleRequest() helper
3. payment-requests POST — accept {bundle:true,trxId} OR {module,trxId}; separate bundle dedup query
4. approve route — loop grant over targets (bundle or single); ledger writes one entry per module
5. auth-token.ts — widen createPaymentRequest signature + MyPaymentRequest type
6. usePendingRequests.ts — add bundle key to PendingMap
7. dashboard — "Buy All — 185 BDT" button + bundle pending state
8. purchase page — ?bundle=1 mode, 185 BDT, all 4 modules summary
9. admin page — LOUD BUNDLE badge (indigo/purple, large, distinct) so admin never confuses 50 BDT payment with 185 BDT bundle
10. .env.local.example — BUNDLE_PRICE=185
11. firestore.indexes.json — uid+isBundle+status composite index
12. build green + lint clean

### Security (unchanged from approved plan)
Amount server-injected from env. usedTrxIds lock unchanged (TrxID-keyed). Atomic grant loop inside runTransaction. Admin UI badge is the amount-enforcement surface (admin eyeballs bKash manually, same trust model as today).