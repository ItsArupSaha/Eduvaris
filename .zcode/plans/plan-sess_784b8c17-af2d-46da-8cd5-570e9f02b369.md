## Goal
Generate a single, self-contained **`FRONDEXAI_MASTER_CONTEXT.md`** at the repo root (`C:\Users\sahaa\Documents\ai-frontdesk-agent\FRONDEXAI_MASTER_CONTEXT.md`) that captures the *actual, current* state of the Frondexai system so any new AI agent can understand 100% of it without re-scanning.

This supersedes the partially-stale `CLAUDE.md` (which still references `nodes.py`, a `role` column, LemonSqueezy-only billing, etc.). The existing `CLAUDE.md` will be left untouched.

## Method (already done)
Three Explore agents mapped backend / frontend / DB+billing+config in parallel, plus I directly read the four most critical files (`services/plans.py`, `routers/paddle_webhook.py`, `services/trial_service.py`, `CLAUDE.md`) to verify exact details.

## File Outline (the document I will write)

A single markdown file (~1,500–2,000 lines) with these sections, in order:

1. **Document header & purpose** — scope, supersedes-CLAUDE.md note, "as-of date 2026-07-21", uncommitted-changes caveat.
2. **TL;DR / 60-second overview** — one-paragraph product pitch, the 6 layers (Vapi / FastAPI+LangGraph / Supabase / Twilio / Google Calendar / Paddle), infra (Railway + Vercel + Supabase).
3. **High-level architecture** — frontend ↔ backend split, request lifecycle diagram (ASCII), dual Supabase-key model (anon+RLS on FE, service_role bypasses RLS on BE), why tenant isolation is enforced in code.
4. **Complete file tree** — annotated tree of `backend/`, `frontend/`, `tests/`, `docs/`, `prospector/`, root configs. Each significant file gets a one-line purpose.
5. **Backend deep dive**
   - Entry point (`main.py`) — lifespan, CORS origins, router mounts, rate limits.
   - Config (`config.py`) — all settings.
   - Every router with a route table: method, path, auth, request model, response, purpose.
   - LangGraph workflow — `AgentState` fields, every node, every edge, conditional routing, the per-turn mechanics, the 4-second timeout, sentinel-message persistence trick.
   - All 6 tools.
   - All services (calendar, vapi, twilio, sms, rag, scheduler, activation, call, client, trial, reminder, plans) with their public functions.
   - Pydantic model catalog.
6. **Frontend deep dive**
   - Build/Vite/TS/Tailwind config.
   - Full route table (18 pages) with guards.
   - State management (React Query + AuthContext + local).
   - API client layer (`api.ts` wrappers + every call).
   - Components/hooks inventory.
   - Paddle.js client integration (Register + Settings).
   - Env vars (`VITE_*`).
7. **Core workflows — step-by-step mechanical traces** (the heart of the doc):
   - **A. Inbound voice call** — Vapi `assistant-request` → HMAC verify → client lookup by phone → trial guard → state load → LangGraph turn → state persist → reply/transfer. Including the greeting → collect_info → booking progression across turns.
   - **B. Booking sub-flow** — slot fetch → selection parse → calendar insert → background SMS + DB write + reminder queue → rollback on DB failure.
   - **C. End-of-call** — resolve client → write call_log → `increment_minutes_used` RPC → missed-call-recovery queue.
   - **D. Self-serve trial signup** — `/register` wizard → Paddle checkout → `register-client` → `verify-trial-checkout` → trial timestamps set → background `run_activation`.
   - **E. Paddle webhook** — signature verify → client lookup → event → status mapping table.
   - **F. Google Calendar OAuth** — connect → callback → encrypted refresh token.
   - **G. APScheduler jobs** — reminders / review requests / missed-call recovery.
8. **Database schema & state**
   - 8 tables (`clients`, `call_logs`, `conversation_state`, `bookings`, `knowledge_chunks`, `reminders_queue`, `sms_optouts`, `admins`) with full column tables, defaults, constraints.
   - Relationships diagram.
   - RLS policy table + the service_role caveat.
   - 3 DB functions (`match_knowledge_chunks`, `increment_minutes_used`, `reset_billing_cycle`).
   - **Tenant isolation model** — `clients.id == auth.uid()`, service_role bypasses RLS, `_require_client_access` is the gate, webhook tenant resolution by phone number.
   - All 24 migrations listed with one-line purpose each.
9. **Billing architecture**
   - Plan table (from `plans.py` — Starter $99/400, Growth $149/700, Pro $199/1300, Custom, $0.20 overage, $100 setup).
   - 7-day card-authenticated free trial mechanics — 50-min cap + 7-day expiry, trial columns, no cron (runtime-only enforcement).
   - Paddle webhook event → DB-status mapping table (subscription.active/trialing, past_due, paused, canceled, transaction.completed, transaction.payment_failed).
   - Cancel (next_billing_period) / reactivate flows.
   - Legacy LemonSqueezy + marketing-only PaymentPage noted.
   - **Flagged discrepancies**: plans.py vs Register/Settings display (Growth 700 vs 1000, Pro 1300 vs 2000); `clients.email` column used but never created by a migration; webhook signature bypass when secret unset; `sms_optouts` RLS gap; no trial-expiry cron.
10. **API contracts** — every internal route with method, path, auth dependency, request payload (Pydantic fields), response shape. Grouped by router.
11. **Environment variables** — full table (backend `os.getenv` + frontend `VITE_*`), name / purpose / where used / required-vs-default. No secret values. Plus per-client DB-stored secrets (Fernet-encrypted refresh token, FSM keys).
12. **Deployment & infra** — Dockerfile, railway.toml, Procfile, frontend vercel.json, CORS allow-list, rate-limit table, no `supabase/config.toml`, migrations run manually via SQL Editor.
13. **Coding standards** — async/await rules, sync-SDK wrapping (`to_thread`), 4-second webhook timeout + transfer fallback, structlog patterns, never-raise SMS/rollback helpers, FastAPI `Depends` for auth, rollback discipline, prompt-injection defense, sentinel messages, cached LLM, Pydantic v2, React Query patterns, `cn()`/Tailwind tokens.
14. **Security model** — auth (Supabase JWT), authz (`_require_auth` / `_require_client_access` / `require_admin`), webhook signatures (Vapi HMAC, Paddle HMAC, LemonSqueezy HMAC, Twilio none), Fernet encryption, dev-bypass token rules, known risks.
15. **Testing** — pytest layout, noted test files.
16. **Known issues, gotchas & launch blockers** — consolidated list (the 5 discrepancies above + the uncommitted `_require_client_access` security fix + missing Paddle Price IDs in FE `.env`).
17. **Glossary** — Vapi, Deepgram, ElevenLabs, A2P 10DLC, FSM, RAG, RLS, sentinel message, etc.
18. **Appendix: uncommitted git changes** — exactly what `M dashboard_api.py`, `M onboarding.py`, `?? plans.py` change and why.

## Conventions for the file
- All code references use `file_path:line` format (clickable).
- ASCII diagrams for architecture + flows.
- Markdown tables for routes, columns, env vars, plans, webhook events.
- Every claim traced to a specific file the agents read; no invented detail.
- Secret values never written — only var names.

## Out of scope
- Will NOT modify `CLAUDE.md` (leave the legacy doc as-is, only note the new doc supersedes it).
- Will NOT change any code, configs, or migrations.
- Will NOT run any commands — single `Write` call to create the markdown file.

## Permission needed
One `Write` tool call to create `C:\Users\sahaa\Documents\ai-frontdesk-agent\FRONDEXAI_MASTER_CONTEXT.md`. No other tools required.