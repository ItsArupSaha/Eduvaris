# OpenAI API key (gpt-4o-mini only — DeepSeek dropped per project decision)
OPENAI_API_KEY=sk-...

# Optional: override defaults from config.ts
# SPIKE_MODEL=gpt-4o-mini
# SPIKE_RUNS_PER_SAMPLE=10
# SPIKE_BAND_GAP_THRESHOLD=1.5
